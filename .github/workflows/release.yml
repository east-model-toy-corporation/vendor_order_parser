name: Create Release

on:
  push:
    tags:
      - 'v*.*.*' # e.g. v1.0.0, v1.2.3

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build executable with PyInstaller
        run: pyinstaller vendor_order_parser.spec

      - name: Package release assets
        run: |
          $asset_name = "vendor_order_parser-${{ github.ref_name }}.zip"
          Compress-Archive -Path dist/vendor_order_parser -DestinationPath $asset_name
          echo "ASSET_NAME=$asset_name" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        shell: powershell

      - name: Create GitHub Release and Upload Asset
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ASSET_NAME }}
